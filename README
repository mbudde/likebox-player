== Likebox Player ==To install Likebox Player make sure you have the following software installed: * Python 2.6 or 2.7 * PyQt4 * python-mpd (`pip install python-mpd`) * nmevent (`pip install nmevent`) * MPDTo run the program execute `run.sh` from the root directory. If the programexits with a socket error, it's because MPD is not running on port 6600 onlocalhost.To install the program run `python setup.py install`.